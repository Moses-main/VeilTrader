/**
 * VeilTrader Extensions
 * 
 * Additional features for hackathon prize tracks
 */

const StETHTreasury = require('./StETHTreasury');
const StatusNetwork = require('./StatusNetwork');
const OlasMarketplace = require('./OlasMarketplace');
const LidoMCP = require('./LidoMCP');

module.exports = {
  StETHTreasury,
  StatusNetwork,
  OlasMarketplace,
  LidoMCP
};
